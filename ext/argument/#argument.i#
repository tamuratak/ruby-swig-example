%module "mylib::argument"

%{

#include "argument.hpp"

%}

namespace Argument {
  class A {
  public:
    A(int);
    ~A();
  };
  %typemap(in) A {
    if(RB_TYPE_P($int, T_FIXNUM)) {
      $1 = NUM2INT(other
    }else{
    }
  }
  void func(int);
};
